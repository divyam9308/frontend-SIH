import { useState } from 'react'; import type { DashboardFilters } from '../types/filters'; const defaults:DashboardFilters={sector:'All Sectors',ministry:'All Ministries',state:'All States',riskLevel:'All Levels',projectStatus:'All',period:'Latest Snapshot'}; export const useFilters=()=>{const [filters,setFilters]=useState(defaults); const setFilter=<K extends keyof DashboardFilters>(key:K,value:DashboardFilters[K])=>setFilters(p=>({...p,[key]:value})); return {filters,setFilter,resetFilters:()=>setFilters(defaults)}}
