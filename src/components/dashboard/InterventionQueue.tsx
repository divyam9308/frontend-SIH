import { Link } from 'react-router-dom'; import type { Project } from '../../types/dashboard'; import { Card } from '../ui/Card'; import { Badge } from '../ui/Badge'; export function InterventionQueue({projects}:{projects:Project[]}){return <Card className="overflow-hidden p-0"><div className="border-b border-slate-100 px-4 py-3"><h2 className="text-xs font-bold tracking-wide text-slate-700">TOP PRIORITY: INTERVENTION QUEUE</h2></div><div className="overflow-x-auto"><table className="w-full min-w-175 text-left text-xs"><thead className="bg-slate-50 text-[10px] uppercase tracking-wide text-slate-500"><tr>{['#','Project Name','Sector','Risk Score','Cost overrun','Delay','Progress','Calibration'].map(x=><th key={x} className="whitespace-nowrap px-3 py-2.5 font-bold">{x}</th>)}</tr></thead><tbody>{projects.map(p=><tr key={p.code ?? p.id} className="border-t border-slate-100"><td className="px-3 py-3 text-slate-500">{p.id}</td><td className="px-3 py-3 font-semibold text-slate-700">{p.code ? <Link to={`/projects/${p.code}`}>{p.name}</Link> : p.name}</td><td className="px-3 py-3 text-slate-600">{p.sector}</td><td className="px-3 py-3"><span className="font-bold text-red-600">{p.riskScore}</span></td><td className="px-3 py-3 text-slate-600">{p.costRisk.toFixed(1)}%</td><td className="px-3 py-3 text-slate-600">{p.scheduleRisk.toFixed(1)} mo</td><td className="px-3 py-3 text-slate-600">{p.progress === null ? 'Not reported' : `${p.progress}%`}</td><td className="px-3 py-3"><Badge tone="warning">{p.warning}</Badge></td></tr>)}</tbody></table></div><Link to="/projects" className="block border-t border-slate-100 px-4 py-3 text-xs font-semibold text-blue-600">View all projects →</Link></Card>}
