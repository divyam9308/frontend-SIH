import { Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis,CartesianGrid } from 'recharts'; import type { RiskTrendPoint } from '../../types/dashboard'; import { Card } from '../ui/Card'; export function RiskTrendChart({data}:{data:RiskTrendPoint[]}){return <Card><h2 className="text-xs font-bold tracking-wide text-slate-700">RISK TREND (High + Critical Projects)</h2><div className="mt-3 h-48"><ResponsiveContainer><LineChart data={data}><CartesianGrid stroke="#edf1f5" vertical={false}/><XAxis dataKey="month" tick={{fontSize:11}} axisLine={false} tickLine={false}/><YAxis tick={{fontSize:11}} axisLine={false} tickLine={false}/><Tooltip/><Line type="monotone" dataKey="value" stroke="#1465d8" strokeWidth={2.5} dot={{r:3,fill:'#1465d8'}}/></LineChart></ResponsiveContainer></div></Card>}
