import type { DashboardKPI } from '../../types/dashboard'; import { Card } from '../ui/Card'; export function KpiCard({item}:{item:DashboardKPI}){const colors={blue:'border-l-blue-600',red:'border-l-red-500',orange:'border-l-amber-500'}[item.tone]; return <Card className={`border-l-4 ${colors} p-4`}><p className="text-[10px] font-bold tracking-wide text-slate-500">{item.title}</p><p className="mt-2 text-2xl font-bold text-slate-800">{item.value}</p><p className={`mt-2 text-xs font-medium ${item.tone==='blue'?'text-emerald-600':'text-red-500'}`}>{item.change}</p></Card>}
