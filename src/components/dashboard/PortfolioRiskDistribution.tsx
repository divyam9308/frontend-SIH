import { Cell,Pie,PieChart,ResponsiveContainer } from 'recharts'; import type { RiskDistribution } from '../../types/dashboard'; import { Card } from '../ui/Card'; export function PortfolioRiskDistribution({data,total}:{data:RiskDistribution[];total:number}){return <Card><h2 className="text-xs font-bold tracking-wide text-slate-700">PORTFOLIO RISK DISTRIBUTION</h2><div className="relative mt-2 h-48"><ResponsiveContainer><PieChart><Pie data={data} dataKey="value" innerRadius={55} outerRadius={80} paddingAngle={2}>{data.map(d=><Cell key={d.name} fill={d.color}/>)}</Pie></PieChart></ResponsiveContainer><div className="pointer-events-none absolute inset-0 flex flex-col items-center justify-center"><b className="text-xl">{total.toLocaleString('en-IN')}</b><span className="text-[10px] text-slate-500">Projects</span></div></div><div className="grid grid-cols-2 gap-2 text-[11px]">{data.map(d=><span key={d.name} className="flex items-center gap-1.5 text-slate-600"><i className="h-2 w-2 rounded-full" style={{background:d.color}}/>{d.name} {d.value}</span>)}</div></Card>}
