import { Select } from '../ui/Select'; import type { DashboardFilters } from '../../types/filters'; export function FilterBar({filters,setFilter,reset}:{filters:DashboardFilters;setFilter:<K extends keyof DashboardFilters>(k:K,v:DashboardFilters[K])=>void;reset:()=>void}){const fields:[keyof DashboardFilters,string,string[]][]=[['sector','Sector',['All Sectors','Railways','Roads','Power','Water']],['ministry','Ministry',['All Ministries','Railways','Road Transport','Power']],['state','State',['All States','Maharashtra','Delhi','Andhra Pradesh']],['riskLevel','Risk Level',['All Levels','Critical','High','Moderate','Low']],['projectStatus','Project Status',['All','On Track','Delayed','At Risk']],['period','Period',['Latest Snapshot','Last Quarter','FY 2026']]];return <div className="flex flex-wrap items-end gap-3 border-b border-slate-200 bg-white px-6 py-3">{fields.map(([key,label,options])=><Select key={key} label={label} value={filters[key]} onChange={v=>setFilter(key,v)} options={options}/>) }<button onClick={reset} className="mb-0.5 text-xs font-semibold text-blue-600 hover:text-blue-800">Reset Filters</button></div>}
