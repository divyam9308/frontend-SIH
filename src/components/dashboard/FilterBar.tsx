import { Select } from '../ui/Select'; import type { DashboardFilters } from '../../types/filters'; export function FilterBar({filters,setFilter,reset,sectors}:{filters:DashboardFilters;setFilter:<K extends keyof DashboardFilters>(k:K,v:DashboardFilters[K])=>void;reset:()=>void;sectors:string[]}){const fields:[keyof DashboardFilters,string,string[]][]=[['sector','Sector',['All Sectors',...sectors]],['riskLevel','Risk Level',['All Levels','Critical','High','Medium','Low']]];return <div className="flex flex-wrap items-end gap-3 border-b border-slate-200 bg-white px-6 py-3">{fields.map(([key,label,options])=><Select key={key} label={label} value={filters[key]} onChange={v=>setFilter(key,v)} options={options}/>) }<button onClick={reset} className="mb-0.5 text-xs font-semibold text-blue-600 hover:text-blue-800">Reset Filters</button></div>}
