/** Tailwind v4 is loaded through the Vite plugin; this config is retained for future theme extensions. */
export default { content: ['./index.html', './src/**/*.{ts,tsx}'], theme: { extend: {} }, plugins: [] }
